# ==============================================================================
# Lab-specific pip requirements
# Install these INSTEAD of the full pyproject.toml in a GCP lab
# ==============================================================================
# 
# Usage in GCP lab terminal:
#   pip install -r lab_test/requirements_lab.txt
#
# These are lighter versions that skip expensive/unavailable services:
#   - FAISS instead of Vertex AI Vector Search
#   - No KFP (run pipelines locally as Python)
#   - Core GCP services only
# ==============================================================================

# ---- GCP Core ----
google-cloud-aiplatform>=1.60.0
google-cloud-storage>=2.16.0
google-cloud-logging>=3.10.0

# ---- LangChain (GCP) ----
langchain>=0.2.0
langchain-google-vertexai>=1.0.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# ---- Local Vector DB (Lab replacement for Vertex AI Vector Search) ----
faiss-cpu>=1.7.4
numpy>=1.24.0

# ---- Config + Validation ----
omegaconf>=2.3.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# ---- Logging ----
loguru>=0.7.0

# ---- Server ----
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0

# ---- Document loading ----
PyPDF2>=3.0.0
unstructured>=0.12.0

# ---- Testing ----
pytest>=8.0.0
requests>=2.31.0

# ---- Optional: ADK for serving (install separately if API key available) ----
# google-adk>=1.0.0
